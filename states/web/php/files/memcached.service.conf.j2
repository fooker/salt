### This file is managed by saltstack - any changes will be overwritten ###

{% import 'hive/init.sls' as hive with context -%}

[Service]
ExecStart=
ExecStart=/usr/bin/memcached \
        -p 11211 \
        -l {{ pillar.peering.interfaces[grains.id].hive.ip4.address }} \
        -m 512

